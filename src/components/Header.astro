---
import HeaderLink from "./HeaderLink.astro";

const { color } = Astro.props;
---

<header
    class="sticky top-0 z-50 bg-primary text-white"
    style={`background-color: ${color};`}
>
    <nav class="container mx-auto p-4 flex justify-between items-center gap-4">
        <a href="/" class="flex items-center gap-2">
            <!-- <div class="hexagon h-10"></div>
                <span class="text-xl leading-5">
                    <span class="font-semibold">Eventos</span>
                    <br /><span class="font-light">de mesa</span>
                </span> -->
            <div class="flex items-center gap-2 text-lg sm:text-xl">
                <div class="hexagon bg-white h-7 sm:h-8"></div>
                CALENDADOS
            </div>
        </a>
        <div class="flex gap-4">
            <HeaderLink href="/">Home</HeaderLink>
            <HeaderLink href="/eventos">Listado</HeaderLink>
            <HeaderLink href="/eventos/proximos">Próximos</HeaderLink>
        </div>
    </nav>
</header>

<style>
    .hexagon {
        aspect-ratio: 1 / cos(30deg);
        clip-path: polygon(50% -50%, 100% 50%, 50% 150%, 0 50%);
    }
</style>
